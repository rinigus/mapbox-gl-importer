layers:
  - id: poi
    table: |
      SELECT geometry, name, COALESCE(NULLIF(name_en, ''), name) AS name_en, class, subclass, rank
      FROM osm_poi_point
      WHERE geometry && !bbox!
    zmin: 14

schema:
  - ./update.sql
