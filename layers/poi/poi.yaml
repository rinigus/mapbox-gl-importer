layers:
  - id: poi
    table: |
      SELECT geometry, name, COALESCE(NULLIF(name_en, ''), name) AS name_en, class, symbol, rank
      FROM osm_poi_point
      WHERE geometry && !bbox!
      {% if z <= 13 %}
      AND rank <= z
      {% endif %}
    zmin: 13

schema:
  - ./update.sql
